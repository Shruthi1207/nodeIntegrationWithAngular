
use cloudgate
db.barriers.drop()
try {
   db.barriers.insertMany( [
{ rank:1,hover:"If the application business sponsor does not provide support/funds/time for cloud migration, then a migration is not feasible.",text:"The business leadership has not provided support for modernization of the application.",grouping:"ent-arch",scores:[1,1,1,1,1,1,1]},
{ rank:20,hover:"Traditional IP communication allows a host to send packets to a single host (unicast transmission) or to all hosts (broadcast transmission). IP multicast provides a third possibility: allowing a host to send packets to a subset of all hosts as a group transmission. Major Cloud Providers do not support Multicast and 3rd party tools must be used. So, an As-Is migration may not be possible. Most likely Multicast is not used in BBY. ",text:"The application requires IP Multicast.",grouping:"ent-arch",scores:[0,0,0,0,0,1,1]},
{ rank:13,hover:"If the application logic is complex and not documented, migration to cloud as-is may not be possible or desired.",text:"The application's business logic is very complex and not documented.",grouping:"ent-arch",scores:[0,0,1,1,1,1,1]},
{ rank:14,hover:"If the application's HA/DR depends on infrastructure capabilities not provided in the cloud, changes to design should be made before migrating to accommodate new hosting platform. As-Is migration may not be possible.",text:"Data Replication for High Availability / Disaster Recovery (HA/DR) using cloud unsupported mechanisms (Oracle RAC / SAN Replication / etc.).",grouping:"ent-arch",scores:[0,0,1,1,1,1,1]},
{ rank:18,hover:"If the applications must keep/use existing code (i.e. because of Intellectual Property, provides competitive advantage, etc.) and the cloud provider does not support or does not make it possible to run that code, replacing with SaaS cloud option may not be available.",text:"The application uses existing code (intellectual property / proprietary) that provides a competitive advantage.",grouping:"ent-arch",scores:[1,1,1,0,1,1,1]},
{ rank:19,hover:"Cloud migration calls for SaaS first, PaaS second, and then IaaS hosting model considerations. If a SaaS offering is not available for the service application provides, or service is not commodity-based, replacing with SaaS may not be an option.",text:"There is no comparable cloud-based SaaS alternative available in the marketplace. If unknown, leave unchecked.",grouping:"ent-arch",scores:[1,1,1,0,1,1,1]},
{ rank:2,hover:"Even though the application can be hosted in Public Cloud, it is viable option to test modernization internally and then migrate to public cloud.",text:"Application is not ready to migrate to Public Cloud without prior modernization in BBY Private PaaS or IaaS",grouping:"ent-arch",scores:[0,0,0,0,1,1,1]},
{ rank:3,hover:"Even though Private Cloud can match some services offered in the Public Cloud, not every service is/can be replicated internally. If your application requires use of a service not available internally Hybrid or Public Clouds are only options.",text:"Application requires features/capabilities  not available internally in BBY Private Cloud",grouping:"ent-arch",scores:[1,1,1,1,1,0,0]},
{ rank:4,hover:"Only AWS has virtual machines with compute capacities larger than half a TB (up to 2 TB of RAM and 128 vCPUs). In order to use public cloud, App will need to change its architecture to utilize smaller VMs or use PaaS/SaaS solutions.",text:"One of the servers used by the app requires more than 512 GB of RAM or more than 64 vCPUs per server.",grouping:"infrastructure",scores:[0,1,1,1,1,1,1]},
{ rank:5,hover:"If the application depends on Hardware Security Module, As-Is migration to Public Cloud may not be possible.",text:"The application is using and depends on Hardware Security Module (HSM).",grouping:"infrastructure",scores:[0,0,0,1,1,1,1]},
{ rank:6,hover:"Win 2k3+, RHEL 5+, CentOS 6+",text:"OS in App server is NOT supported on the cloud (see supported OS in hover text)",grouping:"infrastructure",scores:[0,0,1,1,1,1,1]},
{ rank:7,hover:"Win 2k3+, RHEL 5+, CentOS 6+",text:"OS in Web server is NOT supported on the cloud (see supported OS in hover text)",grouping:"infrastructure",scores:[0,0,1,1,1,1,1]},
{ rank:8,hover:"Win 2k3+, RHEL 5+, CentOS 6+",text:"OS in Integration server is NOT supported on the cloud (see supported OS in hover text)",grouping:"infrastructure",scores:[0,0,1,1,1,1,1]},
{ rank:9,hover:"Win 2k3+, RHEL 5+, CentOS 6+",text:"OS in DB server is NOT supported on the cloud (see supported OS in hover text)",grouping:"infrastructure",scores:[0,0,1,1,1,1,1]},
{ rank:10,hover:"If, for any reason, the application or its components are not and cannot be virtualized, migrating them to Cloud as-is will not be possible.",text:"The server that is hosting the application or its components cannot be virtualized",grouping:"infrastructure",scores:[0,0,1,1,1,0,1]},
{ rank:11,hover:"When it comes to Cloud, assumption should be made that your infrastructure will eventually fail and any High Availability (HA) and Fault Tolerance (FT) requirements application might have should be architected at Application Layer. If your application relies on SLA of the Data Center Infrastructure and requires over 99.95% uptime – Rehosting as-is is not an option since all 3 CSPs have SLAs of 99.95% for their IaaS VMs and only when properly configured.",text:"Application requires better than 99.95% monthly uptime (high availability / SLA), corresponding to approximately 22 minutes of downtime per month.",grouping:"infrastructure",scores:[0,0,1,1,1,1,1]},
{ rank:15,hover:"Whether the software stack involved will work in a virtual machine environment may depend on whether there is use of specialized device drivers or hardware devices that are unlikely to be supported by an IaaS provider.",text:"Application has hardware dependencies (i.e. architected to use a specific GPU (graphic process unit) or network card, uses hardware encryption, uses a hardware XML accelerator, etc.)",grouping:"infrastructure",scores:[0,1,1,1,1,1,1]},
{ rank:16,hover:"If the application uses unsupported or soon-to-be unsupported software, the migration options to IaaS may be limited and not feasible",text:"The application uses End Of Life software.",grouping:"infrastructure",scores:[0,1,1,1,1,1,1]},
{ rank:21,hover:"If intra application components communications require super low latency (sub 2ms), cloud may not be an option for the application without rearchitecting the application / dependencies / integrations to remove latency requirements.",text:"The intra-application components require very low network latency (sub 2 ms).",grouping:"integration",scores:[0,0,0,0,0,1,1]},
{ rank:12,hover:"All BBY applications must use standard Integration Software (ex IBM MQ, rabbitmq, etc…). If the application is not using standard BBY integration software, migration to cloud may not be possible without changing the application integration software.",text:"The application uses non-enterprise standard integration software (COTS components / middleware / queues).",grouping:"integration",scores:[0,0,1,0,0,1,1]},
{ rank:17,hover:"Even though the application architecture where DB hosted on prem and rest of the app hosted in the cloud is supported, BBY Internal Architecture Standard does not recommend separating DB from rest of the Application.",text:"The database can't be hosted in a different location than the rest of the application.",grouping:"integration",scores:[1,1,1,1,0,1,1]}
   ] );
} catch (e) {
   print (e);
}

